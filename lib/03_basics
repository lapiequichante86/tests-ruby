def who_is_bigger(number)
w = number.sort
return w(1)

def reverse_upcase_noLTA (word)
 #wup = word.upcase 
 #wr = word.reverse
 return a.upcase.reverse.delete 'LTA'
end
 

def array_42 (number)
find {|a| a = 42}
end


def magic_array

end
